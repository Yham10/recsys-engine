# ================================================================
# RECSYS ENGINE - Python Dependencies
# Pin versions for reproducibility
# ================================================================

# --- Data & Processing ---
# numpy/pandas/scikit-learn/mlflow/feast are tightly coupled — use floors,
# not exact pins, and let pip resolve a mutually compatible set.
pandas>=2.2.3,<3
numpy>=2.0.0,<3
# 14.0.1 was compiled against the NumPy 1.x C API and crashes under numpy 2.x
# (_ARRAY_API not found). 16.0.0+ added numpy 2 support; keep <21 to avoid
# the feast 0.59.0 pyarrow>=21 requirement.
pyarrow>=16.0.0,<21.0.0
pyspark==3.5.0

# --- Feature Store ---
# Pinned exactly: 0.58.0 is the only version <0.59 (pyarrow<21 requirement)
# in this range; ALL versions 0.50.0-0.58.0 require redis<5 (see redis below).
feast[postgres,redis]==0.58.0

# --- Machine Learning ---
torch>=2.3,<3
scikit-learn>=1.5.0,<2

# --- MLflow ---
mlflow>=2.16.0,<3
boto3==1.34.0          # For S3/MinIO artifact storage

# --- Kafka ---
confluent-kafka==2.3.0

# --- API ---
fastapi==0.108.0
# feast 0.58.0 allows uvicorn 0.30.6-0.34.0
uvicorn[standard]>=0.30.6,<=0.34.0
pydantic>=2.5.3,<3
pydantic-settings>=2.1.0,<3

# --- Database ---
psycopg2-binary==2.9.9
# feast 0.58.0 requires redis<5,>=4.2.2 (via its "redis" extra)
redis>=4.2.2,<5
sqlalchemy==2.0.23

# --- Utilities ---
python-dotenv==1.0.0
pyyaml==6.0.1
loguru==0.7.2           # Better logging than standard logging
tenacity==8.2.3         # Retry logic for resilience
httpx==0.26.0           # Async HTTP client for testing

# --- Testing ---
pytest==7.4.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0