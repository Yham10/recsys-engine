# ================================================================
# Airflow Worker Dependencies
# ================================================================

# ------------------------------------------------------------------
# STEP 1: Pre-pin gRPC BEFORE feast to stop pip backtracking
# ------------------------------------------------------------------
grpcio==1.58.0
grpcio-tools==1.58.0
grpcio-status==1.58.0
grpcio-reflection==1.58.0
protobuf>=4.21.0,<5.0.0

# ------------------------------------------------------------------
# STEP 2: PyTorch CPU-only (smaller + faster download)
# ------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2+cpu

# ------------------------------------------------------------------
# STEP 3: Core ML
# ------------------------------------------------------------------
numpy>=1.23.2,<2
pandas==1.5.3
scikit-learn==1.3.2
pyarrow==14.0.1

# ------------------------------------------------------------------
# STEP 4: Feature Store (after grpc pins)
# ------------------------------------------------------------------
feast[postgres,redis]==0.40.0

# ------------------------------------------------------------------
# STEP 5: MLflow & Cloud
# ------------------------------------------------------------------
mlflow==2.11.3
boto3==1.33.13

# ------------------------------------------------------------------
# STEP 6: Database
# ------------------------------------------------------------------
psycopg2-binary==2.9.9
redis==4.6.0
sqlalchemy==1.4.54

# ------------------------------------------------------------------
# STEP 7: Utilities
# ------------------------------------------------------------------
loguru==0.7.2
tenacity==8.2.3
pyyaml==6.0.1
python-dotenv==1.0.0
faiss-cpu==1.7.4

# ------------------------------------------------------------------
# STEP 8: Airflow Providers
# ------------------------------------------------------------------
apache-airflow-providers-postgres==5.10.0
apache-airflow-providers-redis==3.5.0
apache-airflow-providers-http==4.8.0